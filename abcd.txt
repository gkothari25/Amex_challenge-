#best tracker in opencv is CSRT tracker


dataframe.isnull.sum() = Gives total count of null values in each columns.
acces the month and year in datetime object.

df['year'] = df['ArrivalDate'].dt.year
df['month'] = df['ArrivalDate'].dt.mon



Best way to deal with categorical data
df_with_dummies = pd.get_dummies( df, columns = cols_to_transform )

#check the main difference between iolc and loc
iloc = df.iloc[:3] = prints first 3 row from the Series 
loc = df.loc[:3] = prints values upto 3 [goes to that row number specified]

#check the non numeric coloumns.
campaign_df.select_dtypes(exclude = ["number"])

#find the unique value in the dataframe
[columns.df.nunique()]


Join two dataframe.
pd.merge(left, right, how='inner', on=None, left_on=None, right_on=None,
         left_index=False, right_index=False, sort=True,
         suffixes=('_x', '_y'), copy=True, indicator=False,
         validate=None)

join : {‘inner’, ‘outer’}, default ‘outer’. How to handle indexes on other axis(es).
-----> Outer for union and inner for intersection.

axis = 0 = row
axis = 1 = column

#convert the datatype of a coloumn
df[col].astype(int)

#get the list of all columns datatype
df.dtypes



